# Xcode per-user state — window layout, breakpoints, scheme selection.
# Never shared, changes constantly, causes pointless diffs.
xcuserdata/
*.xcuserstate

# Build products
build/
DerivedData/
*.ipa
*.dSYM.zip

# macOS
.DS_Store
